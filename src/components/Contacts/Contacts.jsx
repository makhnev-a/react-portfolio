import React from "react";
import styles from './Contacts.module.css';

const Contacts = () => {
    return (
        <div>
            Contacts block
        </div>
    );
};

export default Contacts;