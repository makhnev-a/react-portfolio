import React from "react";
import styles from './WorkOptions.module.css';

const WorkOptions = () => {
    return (
        <div>
            Work options block
        </div>
    );
};

export default WorkOptions;